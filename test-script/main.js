// const JWT_SECRET = process.env.JWT_SECRET; 

// console.log(JWT_SECRET); 

// Jwt sign
const x = 20; 
// let text; 

switch (true) {
    case (x > 10):
      text = "True";
      break;
    case (x < 10):
      text = "False";

    default:
      text = "Looking forward to the Weekend";
      break;
}

console.log(text); 